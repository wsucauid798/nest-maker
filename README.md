# Nest Maker

An interactive app where you assemble a bird's nest one stage at a time. Each material you pick is sent to OpenAI along with a reference photo, and the AI renders the finished nest.

```bash
npm install
npm run dev
```

Then open [http://localhost:3000](http://localhost:3000).

Docs live in [docs/](docs/).
